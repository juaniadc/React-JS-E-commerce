import Cart from "./cart.png"

const CartWidget = () => {
    return (
        <button>
            <img src={Cart} alt="Cart" />
        </button>
        
    
    );
};

export default CartWidget;